---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="AI & Automation Enablement - intelio">
<main>
  <!-- HERO SECTION (Dark) -->
  <section class="py-16 sm:py-20 md:py-24 bg-black text-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6">
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-normal mb-4 sm:mb-6">AI & Automation Enablement</h1>
      <p class="text-lg sm:text-xl text-gray-400">Applied where it removes friction or scales decisions.</p>
    </div>
  </section>

  <!-- SECTION: Our Philosophy (Light) -->
  <section class="py-16 sm:py-20 md:py-24 bg-white text-black">
    <div class="max-w-4xl mx-auto px-4 sm:px-6">
      <h2 class="text-2xl sm:text-3xl md:text-4xl font-normal mb-6 sm:mb-8">Our Philosophy</h2>
      <p class="text-xl sm:text-2xl font-semibold text-gray-900 mb-4">AI is not the product.</p>
      <p class="text-xl sm:text-2xl font-semibold text-gray-900 mb-8">Leverage is.</p>
      <p class="text-base sm:text-lg text-gray-700 leading-relaxed mb-6">We apply AI only where it:</p>
      <ul class="space-y-4">
        <li class="flex items-start gap-3 text-gray-700">
          <span class="text-gray-400 mt-1">•</span>
          <span>Reduces manual effort</span>
        </li>
        <li class="flex items-start gap-3 text-gray-700">
          <span class="text-gray-400 mt-1">•</span>
          <span>Improves decision quality</span>
        </li>
        <li class="flex items-start gap-3 text-gray-700">
          <span class="text-gray-400 mt-1">•</span>
          <span>Scales execution</span>
        </li>
        <li class="flex items-start gap-3 text-gray-700">
          <span class="text-gray-400 mt-1">•</span>
          <span>Removes bottlenecks</span>
        </li>
      </ul>
    </div>
  </section>

  <!-- SECTION: Where AI Is Used (Dark) -->
  <section class="py-16 sm:py-20 md:py-24 bg-black text-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6">
      <h2 class="text-2xl sm:text-3xl md:text-4xl font-normal mb-6 sm:mb-8">Where AI Is Used</h2>
      <ul class="space-y-4 mb-8">
        <li class="flex items-start gap-3 text-gray-300">
          <span class="text-gray-500 mt-1">•</span>
          <span>Workflow automation</span>
        </li>
        <li class="flex items-start gap-3 text-gray-300">
          <span class="text-gray-500 mt-1">•</span>
          <span>Intelligent routing and prioritization</span>
        </li>
        <li class="flex items-start gap-3 text-gray-300">
          <span class="text-gray-500 mt-1">•</span>
          <span>Document and data processing</span>
        </li>
        <li class="flex items-start gap-3 text-gray-300">
          <span class="text-gray-500 mt-1">•</span>
          <span>Forecasting and analysis</span>
        </li>
        <li class="flex items-start gap-3 text-gray-300">
          <span class="text-gray-500 mt-1">•</span>
          <span>Internal assistants where appropriate</span>
        </li>
      </ul>
      <p class="text-sm sm:text-base text-gray-500 italic">Always embedded inside real systems—not bolted on.</p>
    </div>
  </section>

  <!-- CTA SECTION (Light) -->
  <section class="py-16 sm:py-20 md:py-24 bg-white text-black">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 text-center">
      <h2 class="text-2xl sm:text-3xl md:text-4xl font-normal mb-8 sm:mb-12">Want to apply AI where it actually matters?</h2>
      <a href="/contact" class="inline-flex items-center justify-center px-8 py-4 bg-black text-white text-base sm:text-lg font-medium rounded-full hover:bg-gray-800 transition-colors min-h-[56px]">
        Work with Intelio
      </a>
    </div>
  </section>
</main>
</Layout>


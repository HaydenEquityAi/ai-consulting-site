---
// Final CTA section for Property Management industry page - Homepage style
---

<section id="cta" class="cre-cta-section">
  <div class="cre-cta-container">
    <!-- LEFT: TEXT CONTENT -->
    <div class="cre-cta-content">
      <h2 class="cre-cta-headline">
        Ready to move faster?
      </h2>
      <p class="cre-cta-description">
        CRE is a game of speed and judgment. The firms that win are the ones that can evaluate deals faster, respond to opportunities quicker, and make decisions with better information. Let's build the systems that give you that advantage.
      </p>
    </div>
    
    <!-- RIGHT: BUTTON -->
    <div class="cre-cta-button-wrapper">
      <a href="/contact" class="cre-btn-contact">Contact Us</a>
    </div>
  </div>
</section>

<style>
  /* CTA Section - Full Width Colored */
  .cre-cta-section {
    background: #0052CC;
    color: #ffffff;
    width: 100%;
    margin: 0;
    padding: clamp(80px, 12vw, 140px) 24px;
  }

  .cre-cta-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 60px;
  }

  .cre-cta-content {
    flex: 1;
    max-width: 700px;
  }

  .cre-cta-headline {
    font-family: 'Figtree', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    font-size: clamp(40px, 5vw, 56px);
    font-weight: 400;
    line-height: 1.2;
    margin: 0 0 24px 0;
    color: #ffffff;
  }

  .cre-cta-description {
    font-size: 18px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.8;
    margin: 0;
  }

  .cre-cta-button-wrapper {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    margin-right: 40px;
  }

  .cre-btn-contact {
    display: inline-block;
    padding: 16px 48px;
    background: #ffffff;
    color: #0052CC;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    border: none;
    border-radius: 2px;
    cursor: pointer;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .cre-btn-contact:hover {
    background: rgba(255, 255, 255, 0.9);
    color: #0052CC;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .cre-cta-container {
      flex-direction: column;
      align-items: flex-start;
      gap: 32px;
    }
    
    .cre-cta-button-wrapper {
      width: 100%;
      justify-content: flex-start;
      margin-right: 0;
    }
    
    .cre-btn-contact {
      width: 100%;
      text-align: center;
    }
  }

  @media (max-width: 768px) {
    .cre-cta-section {
      padding: clamp(60px, 10vw, 100px) 24px;
    }
    
    .cre-cta-content {
      max-width: 100%;
    }
    
    .cre-cta-headline {
      font-size: clamp(32px, 4vw, 48px);
      margin-bottom: 20px;
    }
    
    .cre-cta-description {
      font-size: 16px;
    }
    
    .cre-cta-container {
      gap: 24px;
    }
    
    .cre-btn-contact {
      padding: 14px 40px;
      font-size: 15px;
      width: 100%;
    }
  }
</style>

